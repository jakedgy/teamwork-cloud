apiVersion: v1
kind: Service
metadata:
  name: {{ include "twc-lab.fullname" . }}-simulator
  labels:
    {{- include "twc-lab.labels" . | nindent 4 }}
    app.kubernetes.io/component: simulator
spec:
  type: ClusterIP
  selector:
    {{- include "twc-lab.componentSelectorLabels" (dict "root" . "component" "simulator") | nindent 4 }}
  ports:
    - name: http
      port: 8080
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "twc-lab.fullname" . }}-simulator
  labels:
    {{- include "twc-lab.labels" . | nindent 4 }}
    app.kubernetes.io/component: simulator
spec:
  replicas: {{ .Values.simulator.replicaCount }}
  selector:
    matchLabels:
      {{- include "twc-lab.componentSelectorLabels" (dict "root" . "component" "simulator") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "twc-lab.componentSelectorLabels" (dict "root" . "component" "simulator") | nindent 8 }}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
      containers:
        - name: simulator
          image: "{{ .Values.simulator.image.repository }}:{{ .Values.simulator.image.tag }}"
          imagePullPolicy: {{ .Values.simulator.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          env:
            - name: TWC_LAB_CASSANDRA_HOST
              value: "{{ include "twc-lab.fullname" . }}-cassandra:9042"
            - name: TWC_LAB_ZOOKEEPER_HOST
              value: "{{ include "twc-lab.fullname" . }}-zookeeper:2181"
            - name: TWC_LAB_ARTEMIS_HOST
              value: "{{ include "twc-lab.fullname" . }}-artemis:61616"
            - name: TWC_LAB_ARTEMIS_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "twc-lab.fullname" . }}-artemis
                  key: username
            - name: TWC_LAB_ARTEMIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "twc-lab.fullname" . }}-artemis
                  key: password
            - name: TWC_LAB_CLUSTER_NAME
              value: {{ .Values.clusterName | quote }}
            - name: TWC_LAB_AWS_REGION
              value: {{ .Values.awsRegion | quote }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            {{- toYaml .Values.simulator.resources | nindent 12 }}
